{
  "name": "erp-nexus-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "cd backend && npx prisma generate && node src/server.js",
    "build": "cd backend && npm install && npx prisma generate"
  }
}
